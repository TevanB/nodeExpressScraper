/*const opggScrape = require('opgg-scrape');
var fs = require('fs');
opggScrape.getStats('Tevv', {region: 'na', refresh: false}).then(stats => {
  console.log(stats);
  fs.writeFile('./public/resources/output.json', JSON.stringify(stats));
});
*/
