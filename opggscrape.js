/*const opggScrape = require('opgg-scrape');
var fs = require('fs');
opggScrape.getStats('Tevv', {region: 'na', refresh: false}).then(stats => {
  process.stdout.write(JSON.stringify(stats));
});
setTimeout(() => {}, 3000);
*/
